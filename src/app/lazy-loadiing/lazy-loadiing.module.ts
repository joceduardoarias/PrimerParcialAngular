import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

import { LazyLoadiingRoutingModule } from './lazy-loadiing-routing.module';


@NgModule({
  declarations: [
    
  ],
  imports: [
    CommonModule,
    LazyLoadiingRoutingModule
  ]
})
export class LazyLoadiingModule { }
